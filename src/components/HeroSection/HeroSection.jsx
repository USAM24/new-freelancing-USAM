import Button from '../ui/Button';

const HeroSection = () => {
  return (
    <section>
      <Button variant={'text'} width="sm">
        Button
      </Button>
    </section>
  );
};

export default HeroSection;
